program sumaPrimerosDiez
    [* Este programa calcula la suma de los primeros 10 números enteros *]
    int acumulador;
    {
    acumulador = 0;
    for (int i = 1; i <= 10; i = i + 1) {
        acumulador = acumulador + i;
    };
    println(acumulador);
}